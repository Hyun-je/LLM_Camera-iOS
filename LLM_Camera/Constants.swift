import Foundation

enum AppConstants {
    enum UserDefaults {
        static let savedPromptKey = "savedPrompt"
        static let savedAPIKey = "savedAPIKey"
        static let defaultPrompt = "Please describe this image in detail. What do you see, what's the mood, and what are its special characteristics?"
    }
} 